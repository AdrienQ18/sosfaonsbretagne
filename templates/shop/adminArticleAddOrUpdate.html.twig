{% extends 'base.html.twig' %}
{% block body %}

{{ form_start(formAddArticle) }}

{{ form_widget(formAddArticle) }}

<button type="submit">
    Enregistrer
</button>

{{ form_end(formAddArticle) }}
{% endblock %}
